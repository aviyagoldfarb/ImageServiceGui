<UserControl x:Class="ImageServiceGui.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageServiceGui"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background ="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Logs" FontSize="40" Foreground="Blue" FontWeight="Bold" Margin="4" HorizontalAlignment="Center"/>
        <ListBox Grid.Row="1" Margin="4" ItemsSource="{Binding VM_Logs}" />
        <DataGrid Grid.Row="2" HorizontalAlignment="Stretch" Height="100" Margin="0,0,0,-25" Grid.RowSpan="2" VerticalAlignment="Top" Width="100"/>
    </Grid>
</UserControl>
